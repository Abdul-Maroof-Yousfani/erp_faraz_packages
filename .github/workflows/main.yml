name: Deploy Laravel Project

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # 1. Files upload karna
      - name: Copy files via SFTP
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USER }}
          password: ${{ secrets.SFTP_PASS }}
          port: 22
          source: "."
          target: "/var/www/farazpack/"
          strip_components: 0

      # 2. Permissions aur Laravel setup
      - name: Execute SSH commands
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USER }}
          password: ${{ secrets.SFTP_PASS }}
          script: |
            cd /var/www/farazpack
            
            # Directory structure setup
            mkdir -p storage/framework/{sessions,views,cache}
            mkdir -p bootstrap/cache
            
            # Permissions fix
            chmod -R 775 storage bootstrap/cache
            
            # Composer (Pehle simple try karein, agar fail ho toh full path denge)
            composer install --no-dev --optimize-autoloader --no-interaction || /usr/local/bin/composer install --no-dev --optimize-autoloader --no-interaction
            
            # Laravel Optimization (Fixed syntax)
            if [ -f .env ]; then
                php7.4 artisan config:cache
                php7.4 artisan route:cache
                php7.4 artisan view:cache
            else
                echo ".env file not found, skipping artisan commands"
            fi
